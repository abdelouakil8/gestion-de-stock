"""Service layer — ALL business logic lives here.

Pricing tiers, the minimum-price floor, atomic stock decrement, and
statistics are implemented and enforced in this layer. Routers stay thin,
models stay dumb, and the UI is never trusted for business rules.
"""
